#https://leetcode.com/problems/palindrome-number/

def isPalindromeNumber(x):
    if str(x) == str(x)[::-1]:
        return True
    else:
        return False
    
x = 101
print(isPalindromeNumber(x))
